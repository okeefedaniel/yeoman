from django.urls import path

app_name = 'yeoman'

urlpatterns = [
    # Phase 3+: intake form, dashboard, detail, calendar, map views
    # For now, just a placeholder so the URL conf loads cleanly.
]
